# Build artifacts
*.zip
*.tar.gz

# Vendor — installed via composer install; built into release zip via CI
vendor/

# OS / IDE
.DS_Store
.idea/
*.log
node_modules/

# Dev tools
.env
.env.*
woocommerce-ucp.zip
.phpunit.result.cache
.phpunit.cache/

# License config injected at download time — contains partner key
shopwalk-ai-config.php
woocommerce-ucp-config.php
